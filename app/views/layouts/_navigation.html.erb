<%= link_to "Rails3 Bootstrap Devise Cancan", root_path, :class => 'brand' %>
<ul class="nav">
  <% if user_signed_in? %>
    <li>
    <%= link_to 'Logout', destroy_user_session_path, :method=>'delete' %>        
    </li>
  <% else %>
    <li>
    <%= link_to 'Login', new_user_session_path %>  
    </li>
  <% end %>
  <% if user_signed_in? %>
    <li>
    <%= link_to 'Edit account', edit_user_registration_path %>
    </li>
    <% if current_user.has_role? :admin %>
      <li class="dropdown" >
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
          Admin<i class="icon-user"></i>
        </a>
        <ul class="dropdown-menu" style="z-index: 1000">
          <li>
            <%= link_to 'Admin Users', users_path %>
          </li>
          <li>
            <%= link_to 'Add Products', new_product_path %>
          </li>
          <li>
            <%= link_to 'Unfinished Orders', orders_path %>
          </li>
          <li>
            <%= link_to 'finished Orders', finished_orders_path %>
          </li>
        </ul>
      </li>
    <% end %>
  <% else %>
    <!--
    <li>
    <%= link_to 'Sign up', new_user_registration_path %>
    </li>
    -->
    <% if session[:cart_id] %>
      <li>
      <%= link_to 'my cart', cart_path(session[:cart_id]) %>
      </li>
    <% end %>
  <% end %>
  </ul>
